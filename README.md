# Kodomo-Dragon

```text
        TODO
        
        #GithubService
            psid는 중복되는 고유 무결성에 맞지 않기 때문에 pk로 부적절한 것 같다. sequencne 칼럼 만들어서 auto increment하자
            이중 데코레이터의 동작 순서 알아보기
            페메 보내는 데코레이터 생성하기
            bot_id는 데코레이터에서만 사용하고 함수내부에서는 사용하지 않는데,매개변수를 제거하면 dict 통한 호출에 문제가 발생할까? 생각해보기
            하나의 uuid로 여러 곳에 웹훅 등록할 경우 생각해보기
```